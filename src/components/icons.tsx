/**
 * Central icon set. All icons come from `lucide-react` so we have one
 * consistent, tree-shakeable source. Import from here (not lucide directly)
 * so a future icon-library swap only touches this file.
 */
export {
  TriangleAlert as IconAlert,
  Car as IconCar,
  MapPin as IconPin,
  Eye as IconEye,
  Play as IconPlay,
  Pause as IconPause,
  RefreshCw as IconRefresh,
  Minus as IconMinus,
  X as IconClose,
  GripVertical as IconGrip,
  LayoutGrid as IconLayout,
  ListTree as IconListTree,
  Radar as IconRadar,
  Activity as IconActivity,
  ScrollText as IconLogs,
  ChevronRight as IconChevronRight,
  Trash2 as IconTrash,
  MoveDiagonal2 as IconResize,
  Cctv as IconCamera,
} from "lucide-react";
